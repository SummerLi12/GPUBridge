/* Generated at CMake configure time from backends/vulkan/reduce_sum_f32.spv —
 * see vector_add_f32_spv.h.in / matmul_f32_spv.h.in's header comments for the
 * full rationale. */
#include <stddef.h>
static const _Alignas(4) unsigned char kReduceSumF32Spv[] = { @VULKAN_SPV_HEX@ };
static const size_t kReduceSumF32SpvSize = sizeof(kReduceSumF32Spv);
